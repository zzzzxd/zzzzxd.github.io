---
title: SWAP分区实战
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2022-12-14 17:12:05
subtitle:
tags:
  -	LINUX
categories:
  -	LINUX运维
---

## 概述

swap 交换分区.内存不足的时候临时充当内存.

## 创建swap分区

### 通过文件创建swap分区

```shell
[root@linx01 ~]# dd if=/dev/zero of=/tmp/t count=100 bs=10M
记录了100+0 的读入
记录了100+0 的写出
1048576000字节(1.0 GB)已复制，1.28384 秒，817 MB/秒
[root@linx01 ~]# swapon
NAME      TYPE       SIZE  USED PRIO
/dev/sda3 partition 1020M 43.5M   -2
[root@linx01 ~]# mkswap /tmp/t
正在设置交换空间版本 1，大小 = 1023996 KiB
无标签，UUID=ca9c6c30-32b3-4236-89bc-ee396dff8bec
[root@linx01 ~]# swapon
NAME      TYPE       SIZE  USED PRIO
/dev/sda3 partition 1020M 43.5M   -2
[root@linx01 ~]# swapon /tmp/t
swapon: /tmp/t：不安全的权限 0644，建议使用 0600。
[root@linx01 ~]# swapon
NAME      TYPE       SIZE  USED PRIO
/dev/sda3 partition 1020M 43.5M   -2
/tmp/t    file      1000M    0B   -3

```

### 通过磁盘分区创建swap

```shell
#创建 1G分区
[root@linx01 ~]# fdisk /dev/sdb
欢迎使用 fdisk (util-linux 2.23.2)。

更改将停留在内存中，直到您决定将更改写入磁盘。
使用写入命令前请三思。

命令(输入 m 获取帮助)：n
Partition type:
   p   primary (0 primary, 0 extended, 4 free)
   e   extended
Select (default p):      
Using default response p
分区号 (1-4，默认 1)：
起始 扇区 (2048-10485759，默认为 2048)：
将使用默认值 2048
Last 扇区, +扇区 or +size{K,M,G} (2048-10485759，默认为 10485759)：+1G
分区 1 已设置为 Linux 类型，大小设为 1 GiB

命令(输入 m 获取帮助)：w
The partition table has been altered!

Calling ioctl() to re-read partition table.

WARNING: Re-reading the partition table failed with error 16: 设备或资源忙.
The kernel still uses the old table. The new table will be used at
the next reboot or after you run partprobe(8) or kpartx(8)
正在同步磁盘。
#格式化 swap分区
[root@linx01 ~]# mkswap /dev/sdb1
mkswap: /dev/sdb1: warning: wiping old xfs signature.
正在设置交换空间版本 1，大小 = 5241852 KiB
无标签，UUID=60ad3453-f89f-4c83-867f-68a93e95eccf
[root@linx01 ~]# swapon
NAME      TYPE       SIZE USED PRIO
/dev/sda3 partition 1020M  54M   -2
/tmp/t    file      1000M   0B   -3
#激活交换分区
[root@linx01 ~]# swapon /dev/sdb1
[root@linx01 ~]# swapon
NAME      TYPE       SIZE USED PRIO
/dev/sda3 partition 1020M  54M   -2
/tmp/t    file      1000M   0B   -3
/dev/sdb1 partition    5G   0B   -4

```

## 小结

- 创建swap分区的流程主要就三步： 创建分区（文件或磁盘分区）、格式化swap文件系统、激活swap分区对应的命令是 fdisk(dd)、mkswap、swapon

- swap分区是内存不够用了 没办法的事情，通常情况，我们不会用，尤其云服务器默认就不带swap分区的，因为云服务器扩资源比较容易。不仅如此，如果有发现 swap分区占用，内存还很充足 就要降低swap的使用度在/etc/sysctl.conf 文件里添加如下参数：vm.swappiness=10

  
