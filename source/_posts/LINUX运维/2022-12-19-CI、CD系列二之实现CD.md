---
title: CI、CD系列二之实现CD
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2022-12-19 21:56:33
subtitle:
tags:
  -	LINUX
categories:
  -	LINUX运维
---

## 概述

​	上篇文章我们对CI、CD的概念进行了大致的解释，这次我们就用Jenkins 利用脚本的方式进行自动化部署。在这之前我们再来解释一下。所谓CI就是持续集成，说白了就是你的代码存放在哪，现在企业中gitlab用的较多。我们将一个静态网页项目放在gitlab中。CD就是持续交付，持续部署。这次我们演示持续部署，即如何自动化的将代码从gitlab中拉取然后部署到服务器上。

## 环境准备

​	假设，我们已经开发完毕一套项目。一个简单的静态网站,并搭建一个gitlab 上传。

![image-20221219222439420](image-20221219222439420.png)

## 手动部署脚本实现

​	我们要进行网站的部署了，如果我们没有引入CD的概念。这个过程应该是手动的拉取代码，分发到服务器进行部署等。

```shell
#01. 下载代码 
git clone git@gitlab.danteblog.com:data_dev/monitor_platform.git

#02. 代码打包
tar zcf web_html-$(date +%F).tar.gz web_html/

#03. 推送代码包 scp :推送到web服务器的/opt
for ip in 128 :
	do scp web_html -$(date +%F).tar.gz   172.16.190.${ip}:/opt/pks/ ;done

#04. 解压,修改目录名字,创建软连接
for ip in 7 8
do ssh 172.16.1.$ip
"cd /opt  &&\
tar xf web_html-$(date +%F).tar.gz &&\
mv web_html  we _htmL-$(date +%F) &&\
rm -f /code /html
ln -s /opt/web_html-$(date +%F)  /code/html “
done
```

这需要我们一条一条输入，可以再自动化一点，变成一个脚本

```shell
#! /bin/bash
#author:dante
#desc: 通过脚本,打包,分发,解压,连接,代码 
App_time=`date +%F`
App_home=/opt/
App_dir=/opt 
App_web_servers="172.16.190.130"


app_pack() {
	#01. 克隆代码并打包
	git clone git@gitlab.danteblog.com:data_dev/monitor_platform.git
	cd ${App_home}
	tar zcf ${App_dir}/monitor_platform-${App_time}.tar.gz ./
}

app_scp() {
	#02. 分发代码
	for ip in ${App_web_servers}
	do 
		scp ${App_dir}/monitor_platform-${App_time}.tar.gz  ${ip}:${App_dir}
	done
}

app_deploy() {
	#03. 解压,修改目录名字,创建软连接
	for ip in ${App_web_servers}
	do ssh $ip "cd ${App_dir}  &&\
		tar xf monitor_platform-${App_time}.tar.gz &&\
		mv monitor_platform  monitor_platform-${App_time} &&\
		rm -f /app/dante/monitor &&\
		ln -s ${App_dir}/monitor_platform-${App_time}  /app/dante/monitor "
	done
}
main() {
	app_pack 
	app_scp 
	app_deploy
}
main
```

## 利用Jenkins实现自动部署

其实，上述的脚本已经相对算是自动化了，对运维工作而言已经减轻了很大的负担，但有了Jenkins平台，我们更可以通过图形化的方式，点点的方式来实现整个流程 

<img src="image-20221220115643909.png" alt="image-20221220115643909" style="zoom:50%;" />

 之后就是整个构建的过程

<img src="image-20221220115747642.png" alt="image-20221220115747642" style="zoom:50%;" />

由于我们现在所有的操作都是在shell里写好的,所以只需要在构建时使用shell即可，但我们还是来挨个看一下来熟悉一下jenkins

<img src="image-20221220120157731.png" alt="image-20221220120157731" style="zoom:50%;" />

这的参数化构建用的比较多，可以添加外部参数.

<img src="image-20221220120318524.png" alt="image-20221220120318524" style="zoom: 67%;" />

这的源码管理就是为了整合gitlab等仓库实际工作中也是整合使用的。构建触发器中，可以标记当代码有推送时，自动进行构建 也就是自动进行发布，这个可以在测试环境这样操作，后面我们也会有演示。

<img src="image-20221220124603192.png" alt="image-20221220124603192" style="zoom:50%;" />

到了构建环节了，填入指令即可了。

<img src="image-20221220134820568.png" style="zoom: 67%;" />

<img src="image-20221220134726098.png" alt="image-20221220134726098" style="zoom:67%;" />

