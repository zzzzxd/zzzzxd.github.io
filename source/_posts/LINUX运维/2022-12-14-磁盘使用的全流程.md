---
title: 磁盘使用流程及企业分区方案
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2022-12-14 20:05:15
subtitle:
tags:
  -	LINUX
categories:
  -	LINUX运维
---

## 磁盘使用流程概述

其实磁盘的使用，主要就是三个步骤，分区、格式化、挂载

1. 磁盘分区 :有了一块地,划分隔断. fdisk/parted

2. 格式化（创建文件系统） :对房间进行装修. mkfs

3. 挂载 :安装门窗,可以入住与使用. mount Linux下面设备必须要挂载才能访问与使用,没有挂载相当于房子没 有门窗. 挂载相当于给设备设置了一个入口.

## 实战演示

### 磁盘分区 对新添加的sdb磁盘划分一个分区

```shell

[root@linx01 ~]# fdisk -l

磁盘 /dev/sda：21.5 GB, 21474836480 字节，41943040 个扇区
Units = 扇区 of 1 * 512 = 512 bytes
扇区大小(逻辑/物理)：512 字节 / 512 字节
I/O 大小(最小/最佳)：512 字节 / 512 字节
磁盘标签类型：dos
磁盘标识符：0x000e3ccf

   设备 Boot      Start         End      Blocks   Id  System
/dev/sda1   *        2048     2099199     1048576   83  Linux
/dev/sda2         2099200    37742591    17821696   83  Linux
/dev/sda3        37742592    39831551     1044480   82  Linux swap / Solaris

磁盘 /dev/sdb：5368 MB, 5368709120 字节，10485760 个扇区
Units = 扇区 of 1 * 512 = 512 bytes
扇区大小(逻辑/物理)：512 字节 / 512 字节
I/O 大小(最小/最佳)：512 字节 / 512 字节

[root@linx01 ~]# fdisk /dev/sdb
欢迎使用 fdisk (util-linux 2.23.2)。

更改将停留在内存中，直到您决定将更改写入磁盘。
使用写入命令前请三思。

Device does not contain a recognized partition table
使用磁盘标识符 0x139651f9 创建新的 DOS 磁盘标签。

命令(输入 m 获取帮助)：n
Partition type:
   p   primary (0 primary, 0 extended, 4 free)
   e   extended
Select (default p): p
分区号 (1-4，默认 1)：1
起始 扇区 (2048-10485759，默认为 2048)：
将使用默认值 2048
Last 扇区, +扇区 or +size{K,M,G} (2048-10485759，默认为 10485759)：
将使用默认值 10485759
分区 1 已设置为 Linux 类型，大小设为 5 GiB

命令(输入 m 获取帮助)：w
The partition table has been altered!

Calling ioctl() to re-read partition table.
正在同步磁盘。
[root@linx01 ~]# fdisk -l

磁盘 /dev/sda：21.5 GB, 21474836480 字节，41943040 个扇区
Units = 扇区 of 1 * 512 = 512 bytes
扇区大小(逻辑/物理)：512 字节 / 512 字节
I/O 大小(最小/最佳)：512 字节 / 512 字节
磁盘标签类型：dos
磁盘标识符：0x000e3ccf

   设备 Boot      Start         End      Blocks   Id  System
/dev/sda1   *        2048     2099199     1048576   83  Linux
/dev/sda2         2099200    37742591    17821696   83  Linux
/dev/sda3        37742592    39831551     1044480   82  Linux swap / Solaris

磁盘 /dev/sdb：5368 MB, 5368709120 字节，10485760 个扇区
Units = 扇区 of 1 * 512 = 512 bytes
扇区大小(逻辑/物理)：512 字节 / 512 字节
I/O 大小(最小/最佳)：512 字节 / 512 字节
磁盘标签类型：dos
磁盘标识符：0x139651f9

   设备 Boot      Start         End      Blocks   Id  System
/dev/sdb1            2048    10485759     5241856   83  Linux
[root@linx01 ~]# 

```

### 格式化操作

```shell
[root@linx01 ~]# mkfs.xfs /dev/sdb1
meta-data=/dev/sdb1              isize=512    agcount=4, agsize=327616 blks
         =                       sectsz=512   attr=2, projid32bit=1
         =                       crc=1        finobt=0, sparse=0
data     =                       bsize=4096   blocks=1310464, imaxpct=25
         =                       sunit=0      swidth=0 blks
naming   =version 2              bsize=4096   ascii-ci=0 ftype=1
log      =internal log           bsize=4096   blocks=2560, version=2
         =                       sectsz=512   sunit=0 blks, lazy-count=1
realtime =none                   extsz=4096   blocks=0, rtextents=0
```

### 挂载

```shell
[root@linx01 ~]# mount /dev/sdb1 /mnt
[root@linx01 ~]# df -h
文件系统        容量  已用  可用 已用% 挂载点
devtmpfs        710M     0  710M    0% /dev
tmpfs           721M   12K  721M    1% /dev/shm
tmpfs           721M  9.7M  711M    2% /run
tmpfs           721M     0  721M    0% /sys/fs/cgroup
/dev/sda2        17G  8.1G  8.9G   48% /
/dev/sda1      1014M  138M  877M   14% /boot
tmpfs           145M     0  145M    0% /run/user/0
/dev/sdb1       5.0G   33M  5.0G    1% /mnt
```

#### 永久挂载

上述演示的只是临时挂载，要想永久挂载，一般有两种方法:

1. 修改/etc/rc.local

- 存放的命令,脚本会在开机的时候自动运行.
- 第1次使用,需要给 /etc/rc.d/rc.local 加上执行权限. chmod +x /etc/rc.d/rc.local

  ```shell
  mount /dev/sdb1 /mnt  #写入到/etc/rc.local
  ```

2. 修改/etc/fstab(推荐)

   ![image-20221214214951906](image-20221214214951906.png)

## 磁盘分区相关指令总结

- fdisk
- lsblk
- mount 、umount
- mkfs  mkfs.xfs 、mkfs.ext3、mkfs.ext4

## 企业分区的通用方案

磁盘分区整个过程还是比较简单的，在这总结了 企业中常见的分区方案，给大家做一个建议。

### 物理机

#### 方案01:测试环境，不太重要的环境

| 分区       | 解释                                                         |
| ---------- | ------------------------------------------------------------ |
| /boot 分区 | 用于引导系统启动,linux内核文件。 1G即可（如果需要升级linux内核） (200MB) |
| /swap 分区 | 内存不足的时候，swap	空间临时充当内存使用。<br />内存小于8G swap 可以给内存的1.5倍或2倍。 最大控制在8-16G <br />内存大于8G swap 给8G 8-16G<br />在一些生产环境中为了极致的速度、性能。会关闭<br />注意:swap 云服务器默认没有swap |
| /          | 剩余多少给多少                                               |

#### 方案02:生产环境，有重要数据的环境



| 分区           | 解释                                                         |
| -------------- | ------------------------------------------------------------ |
| /boot 分区     | 用于引导系统启动,linux内核文件。 1G即可（如果需要升级linux内核） (200MB) |
| /swap分区      | 内存不足的时候，swap	空间临时充当内存使用。<br />内存小于8G swap 可以给内存的1.5倍或2倍。 最大控制在8-16G <br />内存大于8G swap 给8G 8-16G<br />在一些生产环境中为了极致的速度、性能。会关闭<br /> |
| / 分区         | 根分区40-100G                                                |
| /data 数据分区 | 重要数据放在/data 目录、分区中。                             |

#### 方案03:可能会有重要数据

|              | 解释                                                         |
| ------------ | ------------------------------------------------------------ |
| /boot 分区   | 用于引导系统启动,linux内核文件。 1G即可（如果需要升级linux内核） (200MB) |
| /swap分区    | 内存不足的时候，swap	空间临时充当内存使用。<br />内存小于8G swap 可以给内存的1.5倍或2倍。 最大控制在8-16G <br />内存大于8G swap 给8G 8-16G<br />在一些生产环境中为了极致的速度、性能。会关闭<br /> |
| / 分区       | 根分区40-100G                                                |
| 剩余暂不分配 |                                                              |

### 云服务器

根据需求，规划好。所需要的磁盘空间。 如果后期添加，扩容云服务器都很方便。

/ 系统分区 根分区 40-100G  /dev/sda

/data 数据分区 数据分区 不限制。



