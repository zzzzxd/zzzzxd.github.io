---
title: (三)HBASE的安装
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2023-02-15 23:17:42
subtitle:
tags: Hbase
categories: HBase
---

```properties
系统版本：centOS7.7
HBASE版本：apache-hbase-1.2.1
HADOOP版本：apache-hadoop-2.7.6
JDK版本：jdk-1.8.0_45
单机安装/伪分布式：1台服务器
全分布式：3台服务器
```

## HBASE单机安装

### 环境准备

1）解压并更名

```shell
[root@node01 ~]# tar -zxvf hbase-1.2.1-bin.tar.gz -C /usr/local/
[root@node01 ~]# cd /usr/local/
[root@node01 local]# mv hbase-1.2.1 hbase
```

2）配置环境变量并重新引导

```shell
[hadoop@node01 local]# vi /etc/profile
export HBASE_HOME=/usr/local/hbase
export PATH=$PATH:$JAVA_HOME/bin:$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$HBASE_HOME/bin

[hadoop@node01 local]# source /etc/profile
```

### 修改 hbase-env.sh

```shell
[hadoop@node01 local]$ vi $HBASE_HOME/conf/hbase-env.sh
#找到下面内容，解开注释，添加具体路径
# The java implementation to use.  Java 1.7+ required.
export JAVA_HOME=/usr/local/jdk

# Tell HBase whether it should manage it's own instance of Zookeeper or not.
export HBASE_MANAGES_ZK=true	 #hbase内置zookeeper开启
```

### 配置hbase-site.xml

```xml
<!-- 属性hbase.rootdir用于指定hbase产生的数据的存储位置 -->
<property>
    <name>hbase.rootdir</name>
    <value>file:///usr/local/hbase/data</value>
</property>
<!-- hbase依赖于zookeeper,需要指定内置zookeeper的数据存储位置 -->
<property>
    <name>hbase.zookeeper.property.dataDir</name>
    <value>/usr/local/hbase/zkdata</value>
</property>
```

### 启动HBase服务

```shell
[root@node01 conf]# start-hbase.sh

[root@node01 hbase]# jps
4593 HMaster
3272 ResourceManager
4666 Jps
2923 NameNode
3116 SecondaryNameNode
```

### hbase的客户端连接

```shell
[root@node01 logs]# hbase shell
hbase(main):001:0> status
1 active master, 0 backup masters, 1 servers, 0 dead, 2.0000 average load
----------------------------------------------------------------------------------------
hbase(main):002:0> version
1.2.1, r8d8a7107dc4ccbf36a92f64675dc60392f85c015, Wed Mar 30 11:19:21 CDT 2016
----------------------------------------------------------------------------------------
hbase(main):003:0> whoami
root (auth:SIMPLE)
    groups: root
---------------------------------------------------------------------------------------- 
hbase(main):004:0> help 
```

## 伪分布式安装

### 情况说明

```
- Hbase的伪分布式，指的是Hbase的相关守护进程运行在一台机器上，只不过每一个守护进程都作为独立的进程运行。
- Hbase的伪分布式数据存储可以基于本地文件系统，也可以基于分布式文件系统。
- 基于分布式文件系统时，hbase使用内置的zookeeper或者用户自己搭建的zookeeper.

下面是使用分布式文件系统和用户自己搭建的zookeeper模式。
```

### 安装步骤

**1）**配置Hbase的环境变量

```shell
[root@node01 ~]$ tar -zxvf hbase-1.2.1-bin.tar.gz -C /url/local/
[root@node01 ~]$ cd /url/local/
[root@node01 local]$ mv hbase-1.2.1 hbase
[root@node01 local]$ vi /etc/profile
.......省略.......
#hbase environment
export HBASE_HOME=/url/local/hbase
export PATH=$HBASE_HOME/bin:$PATH

[root@node01 local]$ source  /etc/profile
[root@node01 local]$ hbase version
```

**2）**修改hbase-env.sh

```shell
[root@node01 local]$ vi $HBASE_HOME/conf/hbase-env.sh
........省略...........
#找到下面内容，解开注释，添加具体路径
# The java implementation to use.  Java 1.7+ required.
export JAVA_HOME=/opt/local/jdk

# Tell HBase whether it should manage it's own instance of Zookeeper or not.
export HBASE_MANAGES_ZK=false       #禁止内置zookeeper
```

**3）**配置hbase-site.xml

```xml
[root@node01 local]$ vi $HBASE_HOME/conf/hbase-site.xml

<configuration>
    <!-- 属性hbase.rootdir用于指定hbase产生的数据的存储位置 -->
    <property>
         <name>hbase.rootdir</name>
         <value>hdfs://node01:8020/hbase</value>
    </property>

    <!-- 指定hbase使用的zookeeper集群 -->
    <property>
            <name>hbase.zookeeper.quorum</name>
            <value>node01:2181,node02:2181,node03:2181</value>
    </property>

    <!-- 开启用hbase集群模式 -->
    <property>
         <name>hbase.cluster.distributed</name>
         <value>true</value>
    </property>

    <!--将属性hbase.unsafe.stream.capability.enforce 改为true -->
    <property>
        <name>hbase.unsafe.stream.capability.enforce</name>
        <value>true</value>
    </property>
</configuration>
```

**4）**启动集群

```properties
1. 注意，如果使用的zookeeper为用户自己安装的，那么需要先启动zookeeper的服务
2. 再启动hbase
[root@node01 conf]# start-hbase.sh
[root@node01 conf]# jps
```

5）hdfs的目录查看

![](006.png)

**6）**(可选)：开启备份master(backup-master)

```shell
[root@node01 local]$ local-master-backup.sh start 2 3 5

说明：2，3，5是端口号16010和16000的端口偏移量，生成的端口号就是：16012/16002  16013/16003  16015/16005

关闭备份master：backup-master:
查看备份master的pid，通过kill命令 来结束进程
方法：jps  或   cat /tmp/hbase-hyxy-num-master.pid | xargs kill -9
```

**7）**(可选)：开启或停止其他RegionServer

```shell
[root@node01 local]$  local-regionservers.sh start 2 3 4 5
[root@node01 local]$  local-regionservers.sh stop [num]
```

## 全分布式安装

### 简介

```properties
HBASE是一个分布式系统
其中有一个管理角色：  HMaster(一般2台，一台active，一台backup)
其他的数据节点角色：  HRegionServer(很多台，看数据容量)

实际上，您需要一个完全分布式的配置来全面测试HBase，并在实际场景中使用它。在分布式配置中，集群包含多个节点，每个节点运行一个或多个HBase守护进程。这些包括主实例和备份主实例、多个ZooKeeper节点和多个RegionServer节点。
```

### 环境准备

1）角色分配

```properties
node01: hmaster 		regionserver 
node02: backup master 	regionserver  
node03: 				regionserver 
```

2） 配置hbase的环境变量

```shell
[root@node01 ~]# tar -zxvf hbase-1.2.1-bin.tar.gz -C /usr/local/
[root@node01 ~]# cd /usr/local/
[root@node01 local]# mv hbase-1.2.1 hbase
[root@node01 local]# vi /etc/profile
.......省略.......
#hbase environment
export HBASE_HOME=/usr/local/hbase
export PATH=$HBASE_HOME/bin:$PATH

[root@node01 local]# source  /etc/profile
[root@node01 local]# hbase version
```

3）配置ssh无密码登陆(一般情况下，都在搭建HDFS时，完成)

4）配置时间同步(<font color='red'>**非常重要**</font>，也应该在搭建HDFS时，完成,时间差不能超过30s)

5）确认hbase-env.sh禁用内置的zookeeper

```shell
[root@node01 local]# vi $HBASE_HOME/conf/hbase-env.sh

#找到下面内容，解开注释，添加具体路径

# The java implementation to use.  Java 1.7+ required.
export JAVA_HOME=/usr/local/jdk

# Tell HBase whether it should manage it's own instance of Zookeeper or not.
export HBASE_MANAGES_ZK=false       #禁止内置zookeeper
```

### 配置HBASE

1) 修改hbase-site.xml

```xml
<configuration>
    <!-- 指定hbase在HDFS上存储的路径 -->
    <property>
        <name>hbase.rootdir</name>
        <value>hdfs://node01:8020/hbase</value>
    </property>
    <!-- 指定hbase是分布式的 -->
    <property>
        <name>hbase.cluster.distributed</name>
        <value>true</value>
    </property>
    <!-- 指定zk的地址，多个用“,”分割 -->
    <property>
        <name>hbase.zookeeper.quorum</name>
        <value>node01:2181,node02:2181,node03:2181</value>
    </property>
     <!--将属性hbase.unsafe.stream.capability.enforce 改为true -->
    <property>
        <name>hbase.unsafe.stream.capability.enforce</name>
        <value>true</value>
    </property>
</configuration>
```

2）配置regionserver所在的节点信息

```shell
[root@node01 apps]#  vi $HBASE_HOME/conf/regionservers

删除localhost
添加:
	node01
    node02
    node03
```

3）配置备份hmaster: 创建backup-masters文件

```shell
[root@node01 local]#  cd $HBASE_HOME/conf/
[root@node01 conf]#  echo "node02">> backup-masters
```

4）将hadoop的core-site.xml和hdfs-site.xml放入conf目录内

```shell
[root@node01 local]#  cd $HADOOP_HOME/etc/hadoop/
[root@node01 hadoop]#  cp core-site.xml hdfs-site.xml $HBASE_HOME/conf/
```

5）将hbase分发到集群其他相关节点上

```sh
[root@node01 local]# scp -r /usr/local/hbase node02:/usr/local
[root@node01 local]# scp -r /usr/local/hbase node03:/usr/local
[root@node01 local]# scp /etc/profile node02:/etc/
[root@node01 local]# scp /etc/profile node03:/etc/

注意：
     重新引导一下各个节点的环境变量配置情况
```

### 启动集群

1）启动集群

```
1. 启动zookeeper集群
2. 启动hdfs集群
3. 启动hbase服务
   
说明： start-hbase.sh脚本开启进程的顺序
    - 1. 主节点上的hmaster
    - 2. regionserver
    - 3. 备份hmaster
```

2）查看hbase

```shell
[root@node01 local]# jps
1888 QuorumPeerMain
2438 ResourceManager
2822 HMaster
2090 NameNode
2284 SecondaryNameNode
3373 Jps
2943 HRegionServer
[root@node01 local]# netstat -nltp | grep 2822
tcp    0    0 ::ffff:192.168.49.250:16000 :::*          LISTEN      2822/java
tcp    0    0 :::16010                    :::*          LISTEN      2822/java
```

3）<font color='red'>查看webui页面</font>

```
http://ip:16010
```

4）<font color='red'>查看zookeeper</font>

5）查看hdfs

6) hbase shell 启动

  ```shell
[root@node01 bin]# ./hbase shell
  ```

7) HBASE的测试

```shell
help '命令组'
e.g. 
help 'create'
```

## regionserver动态上下线

### regionserver动态上线

```
Master使用Zookeeper跟踪RegionServer状态。当某个RegionServer启动时，会首先在Zookeeper上的Server目录下建立代表自己的znode，并获得该znode的独占锁。由于Master订阅了Server目录上的变更信息，当Server目录下的znode出现新增或者删除操作时，Master可以得到来自Zookeeper的实时通知。因此一旦RegionServer上线，Master能马上得到消息。如果负载平衡器开启的话，master会为新的regionserver分配region。
```

**1）**准备节点环境

```
方法1：选择集群中正常工作的一台机器node04
方法2：新搭建一台机器node04，保证jdk,hadoop环境配置，hdfs节点动态上线等一系列注意事项
```

**2）**搭建hbase环境

```
- 1. 配置hbase的环境变量
- 2. 配置hbase的hbase-env.sh文件
- 3. 配置hbase的hbase-site.xml文件 

强调：要与hmaster节点的配置内容一致。当然可以选择copy
```

**3）**在节点上启动regionserver

```shell
[root@node04 conf]# hbase-daemon.sh start regionserver
```

**4）**修改conf/regionservers配置文件

```shell
# 将新增节点主机名，添加到regionservers配置文件中,以便下次启动hbase时，带上node04
[root@node01 conf]# vi regionservers
node01
node02
node03
node04

注意：最好同步一下
```

### regionserver动态下线

```
当regionserver断开与zookeeper的会话时，相应的znode被删除。当master通过zookeeper发现该节点不存在后，就会在平衡器的作用下，重新分配region。

当RegionServer动态下线或者宕机时，它和zookeeper的会话断开，zookeeper自动释放代表这台Server的znode的独占锁，并删除该znode。而Master不断轮询Server目录下znode的锁状态时，如果发现某个RegionServer丢失自己的的独占锁，或者连续几次和RegionServer通信都无法成功，Master就尝试去获取代表这个RegionServer的读写锁。一旦获取成功，就可以确定RegionServer和zookeeper之间的网络断开或RegionServer挂了。此时Master会删除这台RegionServer的对应的znode，开始重新分配region。
	
如果网络短暂出现问题导致RegionServer丢失它的锁，RegionServer重新连接到Zookeeper之后，只要代表它的znode还在，它就会不断尝试这个文件上的锁，一旦获取到，就可以继续提供服务。
```

**1）**选择要下线的机器节点

```
比如，我选择了node01
```

**2）**卸载该节点的region

```shell
[root@node01 conf]# graceful_stop.sh node01

#该节点的region会逐渐迁徙到其他regionserver上
```

**3）**修改conf/regionservers配置文件

```shell
# 删除下线的节点主机名，同步到其他节点上
[root@node01 conf]# vi regionservers
# node01  删除或者注释
node02
node03
node04

注意：最好同步一下
```

# 
