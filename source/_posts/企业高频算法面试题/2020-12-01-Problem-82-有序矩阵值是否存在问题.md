---
title: Problem 82 有序矩阵值是否存在问题
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-12-01 10:19:30
subtitle:
tags:
  -	algorithm
  - Recursion
  -	Trie
categories:
  -	算法
    - 企业面试题
---

### 题目

给定一个每一行有序、每一列也有序，整体可能无序的二维数组
再给定一个数num，
返回二维数组中有没有num这个数

 https://leetcode.com/problems/search-a-2d-matrix-ii/

### 题目解析

- 看到有序，先想二分。每次都能去除掉一部分元素，最终就能判定出

### 代码

```java
class Solution {
  public boolean searchMatrix(int[][] matrix, int target){
        int M = matrix.length,N = matrix[0].length;
        int l = 0,r = N-1;
        while (inEdge(l,r,M,N)){
            if(matrix[l][r] == target) return true;
            else if (matrix[l][r] > target){
                r -= 1;
            }else
                l += 1;
        }
        return false;
    }
    public static boolean inEdge(int l,int r,int m,int n){
        return l >= 0 && l < m && r >= 0 && r < n;
    }
}
```

