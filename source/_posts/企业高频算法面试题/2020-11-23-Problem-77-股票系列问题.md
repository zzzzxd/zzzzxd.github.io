---
title: Problem 77 股票系列问题
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-11-23 22:30:30
subtitle:
tags:
  -	algorithm
categories:
  -	算法
    - 企业面试题
---

### 题目

15节

股票问题1：

https://leetcode.com/problems/best-time-to-buy-and-sell-stock/

股票问题2：

https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/

股票问题3:

https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iii/

股票问题4：

https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iv/

股票问题5：

https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/

股票问题6：

https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-transaction-fee/

### 题目解析

今天来吧股票问题整一个大总结

- 问题一：买一次，卖一次，转化问题，找左边的最小值，与当前值做差，求最大值.
- 问题二：相当于无限次，转化也就是找所有的增长区间段。
- 问题三: 可以操作两次了，最大的收益。
- 问题四：最大可以操作K次，最大收益。

和明显，问题三和问题四实际是一问，解决四就ok，最简单的办法就是dp，dp\[i\]\[k\]的含义是从0～i上完成交易不超过k次的最大利润。那么对i位置，只有两种可能性 要么参与交易，要么不参与。参与了也好说，只可能是卖出，因为是完整的交易，进行斜率优化之后，便可以得出如下代码。

- 问题五:其实本质没变，还是那个dp模型，所以下面的代码几乎一样，只不过稍微调整下，加了一个限制。
- 问题五:暂放。

### 代码

```java
//问题一:
class Solution {
    public static int maxProfit(int[] prices) {
        if (prices.length == 0) return 0;
        int res = 0,min = prices[0];
        for (int i = 1; i < prices.length; i++) {
            min = Math.min(prices[i],min);
            res = Math.max(res,prices[i]-min);
        }
        return res;
    }
}
//问题二:
class Solution {
    public static int maxProfit(int[] prices) {
        int res = 0;
        for(int i = 0;i < prices.length-1;i++){
            res  += Math.max(0,prices[i+1]-prices[i]);
        }
        return res;
    }
}
//问题四:
class Solution {
   public static int maxProfit(int k, int[] prices) {
        int N = prices.length;
        //先进行边界处理
        if(k >= N/2){
            int res = 0;
            //相当于不加限制,找所有的连续增长段
            for (int i = 0; i < prices.length-1; i++) {
                res += Math.max(0,prices[i+1]-prices[i]);
            }
            return res;
        }
        int[][] dp = new int[N][k+1];
        //第i个利用第i-1个
        for(int j = 1;j < k+1;j++){
            int t = -prices[0];
            for(int i = 1; i < N;i++){
                t = Math.max(t,dp[i-1][j-1]-prices[i]);
                dp[i][j] = Math.max(t+prices[i],dp[i-1][j]);
            }
        }
        return dp[N-1][k];
    }
}
//问题三:
class Solution {
   public static int maxProfit(int[] prices) {
        int N = prices.length;
        int[][] dp = new int[N][2+1];
        //第i个利用第i-1个
        for(int j = 1;j < 2+1;j++){
            int t = -prices[0];
            for(int i = 1; i < N ;i++){
                t = Math.max(t,dp[i-1][j-1]-prices[i]);
                dp[i][j] = Math.max(t+prices[i],dp[i-1][j]);
            }
        }
        return dp[N-1][2];
    }
}
//问题五:
class Solution {
    public static int maxProfit(int[] prices) {
        int N = prices.length;
        int[] dp = new int[N]; //表示0~i上无限次交易的最大利润
        if(N <= 1) return 0;
        dp[0] = 0;
        dp[1] = Math.max(0,prices[1]-prices[0]);
        int t = Math.max(-prices[1],-prices[0]);
        for(int i = 2;i < N;i++){
            t = Math.max(t,dp[i-2]-prices[i]);
            dp[i] = Math.max(t+prices[i],dp[i-1]);
        }
        return dp[N-1];
    }

    public static void main(String[] args) {
        System.out.println(maxProfit(new int[]{1}));
    }
}
```

