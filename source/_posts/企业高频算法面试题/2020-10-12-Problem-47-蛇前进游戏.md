---
title: Problem 47 蛇前进游戏
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-10-12 11:01:07
subtitle:
tags:
  -	algorithm
  - Recursion
  -	Trie
categories:
  -	算法
    - 企业面试题
---

### 题目

​	给定一个矩阵matrix，值有正、负、0
蛇可以空降到最左列的任何一个位置，初始增长值是0
蛇每一步可以选择右上、右、右下三个方向的任何一个前进
沿途的数字累加起来，作为增长值；但是蛇一旦增长值为负数，就会死去
蛇有一种能力，可以使用一次：把某个格子里的数变成相反数
蛇可以走到任何格子的时候停止
返回蛇能获得的最大增长值

第8节

### 题目解析

- 考察的是递归，无非是增加了一些条件。我们就按照最普通的递归来写，枚举所有的可能性，找出最大值。这样在数据量小的情况下肯定能过的，如果数据量大，就需要再思考如何优化。

### 代码

```java
public static int snake(int[][] matrix){
        if (matrix == null || matrix.length == 0 || matrix[0].length == 0) {
            return 0;
        }
        int M = matrix.length;
        int N = matrix[0].length;
        int max = Integer.MIN_VALUE;
        for (int i = 0; i < M; i++) {
            int t = process(matrix,i,0,false,0,M,N);
            max = Math.max(t,max);
        }
        return max;
    }
public static int process(int[][] matrix,int i,int j,boolean used,int sum,int M,int N){  //从i,j出发可以获得最大值
        if(!InEdge(i,j,M,N) || sum < 0) return sum;
        int p1=Integer.MIN_VALUE,p2 = Integer.MIN_VALUE,p3 = Integer.MIN_VALUE,p4= Integer.MIN_VALUE,p5 = Integer.MIN_VALUE,p6= Integer.MIN_VALUE,p7 = Integer.MIN_VALUE,
                p8 = Integer.MIN_VALUE,p9 = Integer.MIN_VALUE;

        int p10 = sum + matrix[i][j], p11 = used ? Integer.MIN_VALUE:sum - matrix[i][j];

        if(!used){
            p1 = process(matrix,i-1,j+1,true,sum-matrix[i][j],M,N);
            p2 = process(matrix,i-1,j+1,false,sum+matrix[i][j],M,N);

            p3 = process(matrix,i,j+1,true,sum-matrix[i][j],M,N);
            p4 = process(matrix,i,j+1,false,sum+matrix[i][j],M,N);

            p5 = process(matrix,i+1,j+1,true,sum-matrix[i][j],M,N);
            p6= process(matrix,i+1,j+1,false,sum+matrix[i][j],M,N);
        }else{
            p7 = process(matrix,i-1,j+1,true,sum+matrix[i][j],M,N);
            p8 = process(matrix,i,j+1,true,sum+matrix[i][j],M,N);
            p9 = process(matrix,i+1,j+1,true,sum+matrix[i][j],M,N);
        }
        int max_no_stop = Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(p3,Math.max(p1,p2)),p4),p5),p6),p7),p8),p9);
        return Math.max(Math.max(max_no_stop,p10),p11);
    }
```

