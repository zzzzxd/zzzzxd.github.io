---
title: Problem 68 修复BST
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-11-12 11:29:36
subtitle:
tags:
  -	algorithm
  - Recursion
  -	Trie
categories:
  -	算法
    - 企业面试题
---

### 题目

​	Leetcode原题：

​	https://leetcode.com/problems/recover-binary-search-tree/

### 题目解析

- 题目比较简答，因为BST中序遍历是升序，任意调换两个节点，正常的影响就是把中序中这两个元素进行调换会出现两段降序。case1 还提示了 也会有情况出现一段降序。所以说最多两段降序。我们给交换回来即可。
- 用chi[]数组，0位置代表pre。

### 代码

```java
class Solution {
   public void recoverTree(TreeNode root) { 
          TreeNode[] chs = new TreeNode[3];
          InOrder(root,chs);
          int temp = chs[1].val;
          chs[1].val = chs[2].val;
          chs[2].val = temp;
    }
    static public void InOrder(TreeNode root,TreeNode[] chs){
        if (root == null) return;
        InOrder(root.left,chs);
        if (chs[0] != null){
            if(chs[0].val > root.val) {
                chs[1] = chs[1] == null ? chs[0] : chs[1];
                chs[2] = root;
            }
        }
        chs[0] = root;
        InOrder(root.right,chs);
    }
}
```

