---
title: Problem 81 给数组打补丁问题
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-12-01 23:35:33
subtitle:
tags:
  -	algorithm
categories:
  -	算法
    - 企业面试题
---

### 题目

Leetcode原题：

https://leetcode.com/problems/patching-array/

### 题目解析

- 要求添加数字 使得数组内能累加到[1,n]这类题目也挺常见的，假设有[1,2，3],此时可以拿到的最小值是1，最大值是1+2+3 = 6，因为1，2，3是连续的，所以 [1,6]内的任意一个数字都可以拿到。但此时来了一个5，所以现在能拿到的最大值是6+5= 11,由于5+1 = 6，正好和之前[1,2,3]构成的数组[1~6]可以交接上，所以此时可以拿大的连续数字成了[1~11],此时再来一个7，7+1 = 8，7+11 = 18，所以又可以扩展[1~18],此时再来一个19，19+1 = 20，19+18 = 37，现在由19可以单独填充，正好扩展出[1~37]，但此时若来了39，39+1 =40，此时38就得不到了。

### 代码

```java
class Solution {
public static int minPatches(int[] nums, int n) {
        long range = 0;
        int res = 0;
        int cur = 0;
        while (cur < nums.length){
            if (nums[cur] <= range+1){
                range += nums[cur++];
            }else{
                res++; //补range+1
                range += range+1;
            }
            if(range >= n) return res;
        }
        while (range < n){
            res++;
            range += range+1;
        }
        return res;
    }
}
```

