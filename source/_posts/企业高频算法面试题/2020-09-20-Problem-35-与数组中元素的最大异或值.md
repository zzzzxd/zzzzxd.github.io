---
title: Problem 35 与数组中元素的最大异或值
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-09-20 10:41:36
subtitle:
tags:
  -	algorithm
  - Bit Manipuldate
categories:
  -	算法
    - 企业面试题
---

### 题目

​	https://leetcode.com/problems/maximum-xor-with-an-element-from-array/

### 题目解析

- 这个题还是比较经典的，有两种思路，一种是来个简单的，直接把查询的mi 和 数组都按照从小到大排序，加入前缀树中，代码几乎和34题一样
- 前缀树，节点可以存放一些附加信息，比如以当前路径为前缀的单词个数，当前节点是不是单词等等，这个地方存放以当前节点为前缀树的最小值，只有这棵树的最小值符合条件才可以往下走

### 代码

```java
class Solution {
       public static int[] maximizeXor(int[] nums, int[][] queries) {
        final PrefixTree prefixTree = new PrefixTree();
        for (int i = 0; i < nums.length; i++) {
            prefixTree.addNum(nums[i]);
        }
        final int[] res = new int[queries.length];
        for (int i = 0; i < queries.length; i++) {
           res[i] = prefixTree.getMaxXoR(queries[i][0],queries[i][1]);
        }
        return res;
    }
    public static class PrefixTree{
        public class Node{
            Node[] nodes;
            public int min;
            public Node() {
                nodes = new Node[2];
                min = Integer.MAX_VALUE;
            }
            public Node(int min) {
                this.min = min;
            }
        }
        Node root;
        int size;
        public PrefixTree() {
            root = new Node();
        }
        public void addNum(int num){
            Node cur = root;
            root.min = root.min > num ? num:root.min;
            for (int i = 31; i >= 0; i--) {
                int temp = (num>>i)&1;
                cur.nodes[temp] = cur.nodes[temp] == null ? new Node():cur.nodes[temp];
                cur.nodes[temp].min = cur.nodes[temp].min > num ? num:cur.nodes[temp].min;
                cur = cur.nodes[temp];
            }
        }
        //注意
        public int getMaxXoR(int num,int limit){
            if(root.min > limit) return -1;
            Node cur = root;
            int res = num;
            for (int i = 31;i >=0;i--){
                int temp = (num>>i)&1;
                int expect = temp^1;
                expect ^= ( cur.nodes[expect]!= null && cur.nodes[expect].min <= limit)? 0:1;
                res ^= (expect<<i);
                cur = cur.nodes[expect];
            }
            return res;
        }
        public boolean getNum(int num){
            Node cur = root;
            for (int i = 31; i >= 0; i--) {
                int temp = (num>>i)&1;
                if(cur.nodes[temp] == null){
                    return false;
                }
                cur = cur.nodes[temp];
            }
            return true;
        }
    }
}
```

