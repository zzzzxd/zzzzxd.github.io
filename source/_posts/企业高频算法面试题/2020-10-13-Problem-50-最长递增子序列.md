---
title: Problem 50 最长递增子序列
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-10-13 11:44:14
subtitle:
tags:
  -	algorithm
categories:
  -	算法
    - 企业面试题
---

### 题目

​	经典算法之最长递增子序列

​	Leetcode 原题：

​	https://leetcode.com/problems/longest-increasing-subsequence

### 题目解析

- LIS 经典问题。普通的dp树N^2^的，不容易过，采用end数组的方式实现N*logN的
- End[i]代表所有长度为i +1的递增子序列中的最小结尾。明确end数组含义，开始遍历数组arr，对每个值在end数组中查找第一个单于等于arr[i]的位置end[j]，说明arr[i]无法延伸到j+2长度，而是只能降低j+1递增子序列的末尾值。所以更新end[j]的值，同时记录dp[i]。end数组由含义决定是严格递增的对于end数组使用二分查找。整个过程就是N*logN。
- 实际上dp的记录可以省掉。

### 代码

```java
class Solution {
   public static int lengthOfLIS(int[] arr) {
        if(arr == null || arr.length == 0){
            return 0;
        }
        int[] end = new int[arr.length];
        end[0] = arr[0];
        int valid_loc = 0;
        for(int i = 1;i < arr.length;i++){
            int l = 0,r = valid_loc;
            while (l < r){
                int mid = l + (r-l)/2;
                if(end[mid] >= arr[i]){
                    r = mid;
                }else
                    l = mid +1;
            }
            if(end[l] < arr[i]){
                end[l+1] = arr[i];
                valid_loc++;
            }else{
                end[l] = arr[i]; //更新对应长度的子序列的最小值
            }
        }
        return valid_loc+1;
    }
}
```

