---
title: Problem 1 绳子压点问题
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-07-05 11:06:15
subtitle:
tags:
  -	algorithm
  - Sliding Window
categories:
  -	算法
    - 企业面试题
---

### 题目

给定一个有序数组arr，代表坐落在X轴上的点，给定一个正数K，代表绳子的长度
返回绳子最多压中几个点？
注意：即使绳子边缘处盖住点也算盖住。

### 题目解析

- 有一个比较容易想到的思路就是把绳子看成长度固定的滑动窗口就可以了，依次遍历每个点作为绳子左端点的情况下 能压住几个点即可。

### 代码

```java
public static int maxPoint(int[] arr, int L) {
		int left = 0;
		int right = 0;
		int N = arr.length;
		int max = 0;
		while (left < N) {
			while (right < N && arr[right] - arr[left] <= L) {  //扩展窗口，扫描绳子长度内的点
				right++;
			}
			max = Math.max(max, right - (left++));
		}
		return max;
	}
```

