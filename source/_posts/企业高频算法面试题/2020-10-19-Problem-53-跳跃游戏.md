---
title: Problem 53 跳跃游戏
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2020-10-19 19:30:03
subtitle:
tags:
  -	algorithm
categories:
  -	算法
    - 企业面试题
---

### 题目

Leetcode原题：
https://leetcode.com/problems/jump-game-ii/

补:

https://leetcode.com/problems/jump-game/

### 题目解析

- 这两道题目都非常有特点，考察这种先遍历后更新的技巧，两道题目要一块看。

- 重点理解一下跳跃游戏II的三个变量

  - Step : 当前走的步数
  - cur：当前步数以内能到达的位置
  - next: 如果多走一步 能到达的位置

  那么只有走到cur位置的下一个位置，相当于需要再跳，所以step++, cur = next即可。主要在更新next，保持next在多走一步情况下的最大位置。

- 题目二就是因为要考察最小步数,需要step记录，同时在遍历的同时要想办法记录何时去更新step。

### 代码

```java
class Solution {
    public static int jump(int[] nums) {
        if(nums == null || nums.length == 0) return 0;
        int step = 0;
        int cur = 0;
        int next = 0;
        for (int i = 0; i < nums.length; i++) {
            if (i > cur){
                step++;
                cur = next;
            }
            next = Math.max(nums[i]+i,next);
        }
        return step;

    }
}
----
 class Solution {
       public boolean canJump(int[] nums) {
        int max_pos = 0;
        for (int i = 0; i < nums.length; i++) {
            if(i > max_pos) return false;
            else{
                max_pos = Math.max(i + nums[i],max_pos);
            }
        }
        return true;
    }
}
```

